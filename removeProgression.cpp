/*C - Remove a Progression*/
#include <bits/stdc++.h>

using namespace std;
using Long = long long;

int main(){
	
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	int casos;
	cin >> casos;

	while(casos--){
		int tam;
		int pos;
		cin >> tam >> pos;
		cout << pos * 2 << '\n';
	}

	return 0;
}